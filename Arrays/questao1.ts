

/* function p(num:number):boolean{
    if(num % 2 == 0){
        return true;
    }else{
        return false;
    }
}

let num: number = 17
let numP: boolean = p(num);*/

function p(numP: number): boolean{
    return numP % 2 == 0;
}

const I = (numI: number)=>{
    return numI % 2 != 0;
}